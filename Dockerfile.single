# Single-stage — the naive way: ships node + every library + the source.
# Result: a huge image (app:besar).
FROM node:20
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
EXPOSE 8080
CMD ["npm", "run", "preview"]
